# Go mouseless

```txt
desktop          : use a tiling window manager (e.g. i3, qtile, Hyprland, ...)
terminal emulator: i like one called terminator
browse internet  : firefox + plugin (e.g. trydactil)
software dev     : vscode with plugin 
```
 
